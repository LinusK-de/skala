# Expo/React Native dependency graphs have peer-dep mismatches that npm 7+ treats
# as hard errors. Expo's own tooling (expo install) relies on npm here, so allow
# the looser resolution Expo expects.
legacy-peer-deps=true
